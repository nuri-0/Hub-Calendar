## 업데이트
전체 삭제, 선택 삭제, 일정 선택하여 보기 추가


## 소스코드 설명
일정관리.c : 코드의 int main() 부분이 존재하는 실질적인 메인 코드. 일정관리를 진행하는 부분
test.c : mysql() 함수가 포함된 코드. mysql 쿼리를 진행하고 쿼리 결과를 반환하지 않음. insert, delete 등의 쿼리에 적합함
test_return.c : mysql_r() 함수가 포함된 코드. 쿼리를 진행하고 쿼리 결과를 반환함. select 등의 쿼리에 적합함
달력.c : 추후 일정관리와 연동할 부분으로 아직 사용하지는 않음


## 삭제된 코드
main.c : 사용하지 않아 삭제